version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shop-management-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - shop-network
    environment:
      - NODE_ENV=production

networks:
  shop-network:
    driver: bridge

